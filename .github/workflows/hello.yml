name: Hello World

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: action/checkout@v2
      - name: Set up Python
      - uses action/setup-python@v2
    with:
      prython-version: '3.x'
      -name: Run hello.py
      run: python hello.py

